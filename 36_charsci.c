// WAP to find the ascii value of character entered
#include <stdio.h>
int main()
{
  int ch;
  printf("Enter a integer ");
  scanf("%d",&ch);
  printf("The Character for the value of %d is %c", ch, ch);
return 0;
}