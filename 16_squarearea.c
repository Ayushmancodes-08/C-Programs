// WAP to find area of a square.
#include <stdio.h>
int main()
{
    int s;
    printf("Enter the side of the Square");
    scanf("%d", &s);
    printf("Area of the Square = %d",(s*s));
    return 0;
}