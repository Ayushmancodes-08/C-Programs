//  Write a program to read and print a string using gets() and puts()?
#include <stdio.h>
#include <string.h>
int main()
{
    char ch[20];
 printf("Enter a string:");
 gets(ch);
 puts(ch);
return 0;
}