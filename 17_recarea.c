// WAP to find area of a rectangle.
#include <stdio.h>
int main()
{
    int l,b;
    printf("Enter the length and breath of the Square");
    scanf("%d %d", &l,&b);
    printf("Area of the rectangle = %d",(l*b));;
    return 0;
}