// WAP to accept two numbers and find its addition and multiplication TabLE
#include <stdio.h>
int main()
{
    int a, b, c;
    printf("Enter two numbers");
    scanf("%d %d", &a, &b);
    c = a * b;
    printf("The REsult of multiplication: %d\n", c);
    printf("The REsult of addition: %d", a + b);
    return 0;
}