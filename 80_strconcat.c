// WAP to concatenate two strings
#include <stdio.h>
#include <string.h>
int main()
{
 char str[20]="Ayushman";
 char st[20]="Patra";
 strcat(str,st);
 printf("The Concate string is %s",str);
return 0;
}